# Copy to `.env.supabase` in this folder (gitignored). Never commit `.env.supabase`.
#
# PAT: Dashboard → Account → Access Tokens
# DB password: Project → Settings → Database

SUPABASE_ACCESS_TOKEN=
SUPABASE_DB_PASSWORD=

# Optional (default x7petk@gmail.com for promote script)
# BOOTSTRAP_ADMIN_EMAIL=
